import { PasswordAppDirective } from './password-app.directive';

describe('PasswordAppDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordAppDirective();
    expect(directive).toBeTruthy();
  });
});
