import { GoogleCaptchaDirective } from './google-captcha.directive';

describe('GoogleCaptchaDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleCaptchaDirective();
    expect(directive).toBeTruthy();
  });
});
